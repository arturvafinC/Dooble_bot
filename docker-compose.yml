services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/chat_stats.db
      LOG_FILES_DIR: /app/logs/
    volumes:
      - bot_data:/app/data
      - bot_logs:/app/logs
    restart: unless-stopped

volumes:
  bot_data:
  bot_logs:
